class Hunt {
  constructor(user, quest) {
    this.user = user;
    this.quest = quest;
    this.appendHTML();
  }

  appendHTML() {
    // console.log(this.user);
    // console.log(this.quest);
  }
}
